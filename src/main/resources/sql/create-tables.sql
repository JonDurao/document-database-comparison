CREATE TABLE AREAS (ID BIGINT NOT NULL, AREA JSONB, CONSTRAINT areas_pkey PRIMARY KEY (ID));
CREATE TABLE ARTISTS (ID BIGINT NOT NULL, ARTIST JSONB, CONSTRAINT artists_pkey PRIMARY KEY (ID));
CREATE TABLE FORMATS (ID BIGINT NOT NULL, FORMAT JSONB, CONSTRAINT formats_pkey PRIMARY KEY (ID));
CREATE TABLE LABELS (ID BIGINT NOT NULL, LABEL JSONB, CONSTRAINT labels_pkey PRIMARY KEY (ID));
CREATE TABLE LANGUAGES (ID BIGINT NOT NULL, LANGUAGE JSONB, CONSTRAINT languages_pkey PRIMARY KEY (ID));
CREATE TABLE MEDIUMS (ID BIGINT NOT NULL, MEDIUM JSONB, CONSTRAINT mediums_pkey PRIMARY KEY (ID));
CREATE TABLE PLACES (ID BIGINT NOT NULL, PLACE JSONB, CONSTRAINT places_pkey PRIMARY KEY (ID));
CREATE TABLE RECORDS (ID BIGINT NOT NULL, RECORD JSONB, CONSTRAINT records_pkey PRIMARY KEY (ID));
CREATE TABLE RELEASES (ID BIGINT NOT NULL, RELEASE JSONB, CONSTRAINT releases_pkey PRIMARY KEY (ID));
CREATE TABLE TRACKS (ID BIGINT NOT NULL, TRACK JSONB, CONSTRAINT tracks_pkey PRIMARY KEY (ID));